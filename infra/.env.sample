# ===========================================
# Vinyl Vault Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# -------------------------------------------
# GitHub OAuth Configuration
# -------------------------------------------
# Create a GitHub OAuth App at: https://github.com/settings/developers
# See docs/GITHUB_OAUTH_SETUP.md for step-by-step instructions

GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# Callback URL must match exactly what you configure in GitHub OAuth App settings
# For local development:
GITHUB_CALLBACK_URL=http://localhost:3001/auth/github/callback
# For production (replace with your domain):
# GITHUB_CALLBACK_URL=https://yourdomain.com/auth/github/callback

# -------------------------------------------
# Security Secrets
# -------------------------------------------
# Generate secure random strings for production!
# Example: openssl rand -base64 32

# Secret for signing JWTs (BFF to Backend communication)
JWT_SECRET=change_this_to_a_secure_random_string_in_production

# JWT expiration time (e.g., 15m, 1h, 1d)
JWT_EXPIRES_IN=15m

# Secret for signing session cookies
SESSION_SECRET=change_this_to_another_secure_random_string

# -------------------------------------------
# MongoDB Configuration
# -------------------------------------------
# Local development (default)
MONGODB_URI=mongodb://localhost:27017/vinylvault

# Docker development
# MONGODB_URI=mongodb://mongodb:27017/vinylvault

# -------------------------------------------
# Service Ports
# -------------------------------------------
VITE_PORT=3000
BFF_PORT=3001
BACKEND_PORT=4000

# -------------------------------------------
# Service URLs
# -------------------------------------------
BACKEND_URL=http://localhost:4000/graphql
VITE_GRAPHQL_ENDPOINT=http://localhost:3001/graphql

# -------------------------------------------
# External Music APIs (Optional)
# -------------------------------------------
DISCOGS_API_KEY=
DISCOGS_API_SECRET=
MUSICBRAINZ_USER_AGENT=VinylVault/0.1.0 (your@email.com)

# -------------------------------------------
# Node Environment
# -------------------------------------------
NODE_ENV=development